#include<stdio.h>
int main() {
    int num1, num2;
    scanf("%d %d", &num, &num2);
    if(num >= 18 && num2 == 1){
        printf("Eligible");
    }
    else {
        printf("Not Eligible");
    }
} 