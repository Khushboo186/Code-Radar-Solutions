#include<stdio.h>
int main() {
    int num;
    scanf("%d", &num);
    if(num >= 18 && citizen status == 1){
        printf("Eligible");
    }
    else {
        printf("Not Eligible");
    }
} 